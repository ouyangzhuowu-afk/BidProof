apiVersion: v1
kind: Service
metadata:
  name: {{ include "bidproof.name" . }}
  labels:
    {{- include "bidproof.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "bidproof.name" . }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
