# BidProof 企业能力补齐验收

日期：2026-08-27

## 本轮完成

- 公网 `bidproof.marketcase.net` 已切换到最新认证代码；未登录访问任务 API 返回 `401`，伪造 `X-Workspace-ID` / `X-User-Role` 也返回 `401`。
- PDF、DOCX、XLSX、PPTX、TXT、MD 的报告字段统一使用“定位”，不再把段落、工作表单元格、幻灯片文本块标为页码。
- 扫描作业展示阶段进度、进度条、失败原因和重试入口；解析失败不会残留为运行中。
- 工作台提供逾期整改和失败扫描作业的站内提醒；提醒不等同于邮件、企业微信或飞书通知。
- 整改行动支持关联要求项、负责人、截止日期、状态、逾期提示和审计留痕。
- 报告字段使用“招标定位”“企业证据定位”；非 PDF 文档使用段落、工作表、幻灯片或行定位。
- 准确度页面仅显示未校准匹配计数和覆盖状态，不把启发式分值伪装成概率。
- 原始材料可按 `TENDER-001` / `EVD-*` 安全下载；下载按工作区权限校验并限制在任务目录内。
- 扫描作业支持取消；取消状态写入数据库并从可恢复队列排除；账号改密会撤销既有会话。
- 真实项目备份已创建并验证：数据库完整性通过，备份清单包含 40 个上传文件。
- 隔离恢复演练通过：恢复 23 个任务、40 个上传文件，`PRAGMA integrity_check=ok`。

## 验证结果

- `node --check static/app.js`：通过
- `pytest -q`：`96 passed`
- 新增缺口回归：文件下载、作业取消、改密撤销会话、准确度分母与复核完整性均通过。
- `python -m app.workflow check`：`PASS`
- 本地 `/api/auth/status`：`setup_required=true, authenticated=false`
- 公网 `/api/auth/status`：`setup_required=true, authenticated=false`
- 本地/公网伪造身份头访问 `/api/runs`：`401`

## 仍然明确未完成

- 真实企业任务台账仍为 0，正式召回率、漏报率、误报率和付款意愿没有可计量结果。
- 当前公网仍是本机 SQLite + Cloudflare Tunnel，不是高可用托管生产环境。
- 密码找回、MFA/SSO、外部通知、对象存储、集中监控和计费仍属于后续生产化工作。
- 门禁/哈希复核按用户要求暂后置。

结论：BidProof 当前是“认证的企业试点版”，不是已完成商业验收的成熟生产产品。下一最佳行动仍是 T-005 真实企业任务试运行。
