{
  "name": "bidproof-frontend",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "tsc -p jsconfig.json --noEmit --allowJs --checkJs --pretty false"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
